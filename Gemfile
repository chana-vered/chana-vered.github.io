# Only needed to preview the site on your own machine:
#   bundle install
#   bundle exec jekyll serve   ->  http://localhost:4000
#
# GitHub Pages does not use this file; it builds the site itself.

source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
